#!/bin/sh

alias ls="eza -1 --icons=always --color=always"
alias ll='eza -al --icons=always --color=always'
alias la='eza -A --icons=always --color=always'
alias l='eza -G --icons=always --color=always'
alias vi='nvim'
alias vim='nvim'
alias mysql="mariadb"
# alias tmux='tmux -f ${XDG_CONFIG_HOME}/tmux/tmux.conf'
