vim.pack.add({ { src = 'https://github.com/windwp/nvim-ts-autotag', name = "nvim-ts-autotag" } })

require('nvim-ts-autotag').setup({
    -- The default settings are excellent and don't require changes for your use case.
})
